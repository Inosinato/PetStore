webdriver {
  driver = "chrome"
}

serenity {
  project.name = "PetStore"

   # Enable report generation and specify the directory
    report.enabled = true
    report.dir = target/serenity-reports
}
